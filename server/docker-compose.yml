services:
  websocket-server:
    build: .
    container_name: chat-app-websocket
    ports:
      - "8080:8080"
    environment:
      - PORT=8080
      - NODE_ENV=production
    restart: unless-stopped
    networks:
      - chat-network

networks:
  chat-network:
    driver: bridge
